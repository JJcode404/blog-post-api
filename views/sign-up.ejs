<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/sign-up.css" />
    <link rel="icon" href="/images/cloud.svg" type="image/svg+xml" />
    <title>Sign-up Page</title>
  </head>
  <body>
    <div class="sign-up-form">
      <div class="details">
        <div class="top">
          <h2>Hello!</h2>
          <p>Please signup to continue</p>
        </div>
        <form action="/sign-up" method="post">
          <div class="user-inputs">
            <div class="fullname input">
              <label for="fullname">Fullname</label>
              <input
                type="text"
                id="fullname"
                name="fullname"
                value="<%= typeof oldInput !== 'undefined' ? oldInput.fullname : '' %>"
                placeholder="Full Name"
              />
            </div>
            <div class="email input">
              <label for="email">Email</label>
              <input
                type="email"
                id="email"
                name="email"
                value="<%= typeof oldInput !== 'undefined' ? oldInput.email : '' %>"
                placeholder="you@example.com"
              />
            </div>
            <div class="password input validate" id="matchpass">
              <label for="passwordDls">password</label>
              <input
                type="password"
                id="passwordDls"
                name="password"
                placeholder="password"
                required
              />
            </div>
            <div class="confirm-password input validate">
              <label for="cfrmpassord">confirm password</label>
              <input
                type="password"
                id="cfrmpassord"
                name="cfrmpassord"
                placeholder="confirm password"
                required
              />
            </div>
          </div>
          <p>Already have an account?</p>
          <a href="/login">Sign In</a>
          <button type="submit">Sign Up</button>
          <%- include("errors.ejs") %>
        </form>
      </div>
    </div>
  </body>
</html>
